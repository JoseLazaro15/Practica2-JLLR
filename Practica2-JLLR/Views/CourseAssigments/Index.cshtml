@model IEnumerable<Practica2_JLLR.Models.CourseAssigment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instructor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Instructor.InstructorID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseAssigmentID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CourseAssigmentID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseAssigmentID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
